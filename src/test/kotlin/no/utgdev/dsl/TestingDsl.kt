fun `and the credentials were valid, then the app`(function: () -> Unit): Nothing = TODO()
fun `and clicked the login button`(function: () -> Unit): Nothing = TODO()
fun `when user entered credentials`(function: () -> Unit): Nothing = TODO()
fun should(s: String, function: () -> Unit): Nothing = TODO()

fun main() {
    `when user entered credentials` {
        `and clicked the login button` {
            `and the credentials were valid, then the app` {
                should("switch to main flow") {
                    // testing logic goes here :-)
                }
                // more should blocks ...
            }
            // more contexts ...
        }
    }
}
